<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item android:id="@+id/courses" android:icon="@android:drawable/ic_menu_camera"
            android:title="Courses" />
        <item android:id="@+id/lexicon" android:icon="@android:drawable/ic_menu_gallery"
            android:title="Lexicon" />
    </group>

    <item android:title="About">
        <menu>

            <item android:id="@+id/About" android:icon="@android:drawable/ic_menu_send"
                android:title="About" />
        </menu>
    </item>

</menu>
